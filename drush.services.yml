services:
  cypress.commands:
    class: Drupal\cypress\Commands\CypressCommands
    arguments:
      - '@cypress'
      - '@cypress.realpath.test_directories'
      - '@app.root'
    tags:
      - { name: drush.command }
